import {COLORS, FONTS, RADIUS} from '../constants/style.constants';

/**
 * Form elements
 */
export const baseFormStyles = {
  textInput: {
    borderWidth: 1,
    borderColor: COLORS.greyLight,
    padding: 15,
    marginVertical: 10,
    borderRadius: RADIUS.md,
    height: 55,
    paddingVertical: 0,
    backgroundColor: 'white',
    fontSize: FONTS.size.md,
  },
};
